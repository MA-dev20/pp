<section id="count_channel" data-game-id="<%= @game.id %>">
<div id="vote"></div>
<section id="head">
    Wer ist als nächtes dran?
</section>
<section id="middle">
  <div class="circle-left">
      <%= image_tag @turn1.findUser.avatar.quad.url, class: 'pulse' %>
  </div>
  <div class="circle-right">
      <%= image_tag @turn2.findUser.avatar.quad.url, class: 'pulse' %>
  </div>
</section>
<section id="skip">
   Skip mit Leertaste
</section>
</section>
<%= audio_tag("choose/slot.mp3", id: 'slot') %>

<script>
    document.getElementById('slot').play(); 
     $(window).keypress(function (e) {

        if (e.key === ' ' || e.key === 'Spacebar') {
            window.location.replace('<%= gda_turn_path %>');
        };
    });
</script>