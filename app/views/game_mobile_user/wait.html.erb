<%= render partial: 'game_mobile_admin/header' , locals: {can: false}%>
<div class="head">
    <h1 class="nunito-bold">SPIEL BEIGETRETEN!</h1>
</div>
<div class="middle">
    <%if @user.avatar.quad.url%>
        <%= image_tag @user.avatar.quad.url, class: 'mid-img green pulse' %>
    <%else%>
        <%= image_tag 'logo.png', class: 'mid-img green pulse' %>
    <%end%>
</div>

<div class="bottom nunito-regular">
    Gleich geht's los...
</div>