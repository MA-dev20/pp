<div id="count_channel" data-game-id="<%= @game.id %>" >
<%= render partial: 'header' , locals: {can: false}%>
<div class="head">
    <h1>
    <% if @admin == @cur_user %>
        <%= @admin.fname %>, du wirst bewertet!
    <% else %>
        Bewertung abgegeben!
    <% end %>
    </h1>
</div>
<div class="middle" id="middle">
    <div class="row"><div class="col-12"><%= @count %></div></div>
</div>
<div class="bottom">
    <%= link_to 'weiter!', gma_rating_path %>
</div>
</div>