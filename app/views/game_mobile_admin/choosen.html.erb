<%= render partial: 'header' , locals: {can: false}%>
<div class="head">
    <h1 class="nunito-bold">Du hast für <%= @turn.findUser.fname %> gestimmt!</h1>
</div>
<div class="middle">
    <%= image_tag @turn.findUser.avatar.quad.url, class: 'mid-img green pulse' %>
</div>
<div class="bottom">
    <%= link_to 'weiter!', gma_turn_url , id: 'play', class: "nunito-regular" %>
</div>
<script>
	checkOrientation();
 	window.addEventListener('resize', checkOrientation);
</script>