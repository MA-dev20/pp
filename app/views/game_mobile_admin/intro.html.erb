<%= render partial: 'header' , locals: {can: false}%>
<div class="head">
    <% if @video %>
    	<h1 class="nunito-bold">Video läuft...</h1>
    <% else %>
    	<h1 class="nunito-bold">Intro läuft...</h1>
    <% end %>
</div>
<div class="middle">
    <%= image_tag @admin.avatar.quad.url || 'game/wolf_button.png', class: 'mid-img green pulse' %>
</div>
<div class="bottom">
    <%= link_to 'skip', gma_choose_path, class: 'nunito-regular' %>
</div>
<script>
	checkOrientation();
 	window.addEventListener('resize', checkOrientation);
</script>