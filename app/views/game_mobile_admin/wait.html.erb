<section id="count_channel" data-game-id="<%= @game.id %>">
<section id="content">
    <div class="head">
        Alle beigetreten?
    </div>
    <div class="middle">
      <% if @admin.avatar? %>
          <%= image_tag @admin.avatar.quad.url, class: 'mid-img green' %>
      <% else %>
       <div class="mid-img green">
           <%= image_tag 'game/wolf_button.png', class: 'wolf' %>
       </div>
       <% end %>
    </div>
    <div class="bottom">
       <%= link_to 'starten!', gma_choose_path %>
    </div>
</section>
  <div id="modal">
  <%= render partial: 'shared/user_accept_modal' %>
  </div>
</div>
<% if @game %>
  <div id='game_channel' data-game-id="<%= @game.id %>"></div>
<% end %>
<%= audio_tag('wait/ringdoor.mp3', id: 'ringdoor') %>

<%= content_for :meta do %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<% end %>