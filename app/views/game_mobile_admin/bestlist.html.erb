<%= render partial: 'header' , locals: {can: false}%>
<div class="head">
    <h1>Tolle Runde!</h1>
</div>
<div class="middle">
    <%= image_tag @admin.avatar.quad.url || 'defaults/wolf.jpg', class: 'mid-img green pulse' %>
</div>
<div class="bottom">
   <a href="<%= gma_replay_path %>" class="pol-bg" style="display: inline;"><i class="fas fa-redo"></i></a>
   <a href="<%= gma_ended_game_path %>" class="pol-bg" style="display: inline;" data-confirm="Bist du sicher, dass du keine neue Runde starten möchtest?"><i class="fas fa-times"></i></a>
</div>
<script>
	checkOrientation();
 	window.addEventListener('resize', checkOrientation);
</script>