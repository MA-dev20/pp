<br>
    <br>
    <%= form_for(@admin, as: Admin, url: verify_token_path) do |f| %>
        <%= hidden_field_tag "vid_token", @admin.vid_token %>
        <%= text_area_tag :token , @admin.token %>
        <%= f.submit 'Enter  Code', class: 'btn btn-links' %>
    <%end%>
