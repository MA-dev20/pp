<!DOCTYPE html>
<html lang="de">
<head>
    <title>Peterpitch</title>
    <meta name="theme-color" content="#1dacf1"/>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>

    <%= stylesheet_link_tag    'game_desktop/main', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'game_desktop/main', 'data-turbolinks-track': 'reload' %>
</head>

<body>
    <section id="content">
        <%= yield %>
    </section>
    <!-- Modal -->
    <div class="modal fade modal-email" id="myModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog ml-3 modal-dialog-centered mr-auto" role="document">
    <div class="modal-content">
        <div class="modal-header p-0">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
        </div>
        <div class="modal-body">
            <% current_admin.users.where(status: "pending").each do |user| %>
                <div class="mail-box">
                <p class="email-add"><%= user.email %></p>
                
                <div class="mail-btn">
                <%= link_to "Accept", accept_user_path(user_id: user.id), method: :patch, params: { status: 1 }, class: "btn btn-secondary btn-accept" %>
                <%= link_to "Reject", reject_user_path(user_id: user.id), method: :patch, params: { status: 2 }, class: "btn btn-primary btn-reject" %>

                  <!--- <button type="button" class="btn btn-secondary btn-accept">Accept</button>
                    <button type="button" class="btn btn-primary btn-reject">Reject</button>-->
                </div>
                </div>
            <%end%>
        </div>
    </div>
    </div>
    </div>

</body>
</html>
